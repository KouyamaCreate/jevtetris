# DESIGN.md

## Tetris (`/`)

- 白 / 黒を基調に、ニュートラルな面、1px境界線、控えめな角丸で盤面を際立たせる。
- Light / Darkは`next-themes`で切り替え、選択を保持する。強いアクセントは操作中のボタンと状態表示に限る。
- 7種のピースには標準色を維持する。Iはcyan、Oはyellow、Tはpurple、Sはgreen、Zはred、Jはblue、Lはorange。スコア・操作・情報パネルは無彩色にする。
- ユーザーとJevの盤面を横並びにする。盤面の幅は画面高に合わせて抑え、通常のデスクトップ画面では20行全体を見せる。狭い画面では2面を小さく並べ、操作ボタンをタッチしやすくする。
- Start / Pause / Resetは盤面より上に置く。スコア、ライン数、レベル、受信予定、NEXT / HOLDを各盤面の近くに置く。
- Jev HUDは情報量を削らず、より透過した暗色面、1pxシアン境界、軽いblur、低不透明度のカードで背面のセルとグリッドを見せる。試合終了は固定モーダルにせず、試合バーのインライン結果表示で盤面を覆わない。
- 落下は一定速度のセル移動、回転は短い回転遷移、ライン消去は短い点滅とブロック落下で表現する。装飾的な大きな動きは避け、`prefers-reduced-motion`に従う。
- 情報ラベルは小さなモノスペース体、説明は日本語のサンセリフ体で読みやすくする。キーボード操作とタッチボタンの両方を用意する。
- Jev HUDの判断文は省略記号で隠さず、幅に応じて折り返す。狭い画面では判断カードを縦積みにして全文を読めるようにする。
- Jevへの接続はプロバイダを固定しすぎず、サーバー側の共通アダプターからTypeSafe、Vercel、OpenRouter、Cloudflareを選べるようにする。キーや接続状態は画面へ出さない。
